package com.stelle.libs.mvi

/**
 * A marker interface for classes that represent the state in an MVI (Model-View-Intent) architecture.
 *
 * State classes should be immutable data holders (e.g., a data class) that describe the UI
 * at a specific point in time.
 */
interface StelleState